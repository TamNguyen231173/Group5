import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    switchContainer: {
        paddingVertical: 2,
        paddingHorizontal: 2,
        borderRadius: 36.5,
    },
    disabled: { opacity: 0.5 },
});
