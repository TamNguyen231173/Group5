import { Block, Container, Text } from "@components"


export const Video = () =>{
    return(
       
            <Block alignCenter flex justifyCenter backgroundColor="#ffffff">
                <Text size={20} lineHeight={20}>Video Screen</Text>
            </Block>
        
    )
}