import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    stepStyle: {
        overflow: 'hidden',
        width: '100%',
        position: 'absolute',
        left: 0,
        bottom: 0,
    },
});
